# PyTorch packages to be installed from the PyTorch package index. Note the we are using
# the CPU-only version for the devcontainer.

--index-url https://download.pytorch.org/whl/cpu

torch==2.9.1+cpu
torchaudio==2.9.1
torchvision==0.24.1
